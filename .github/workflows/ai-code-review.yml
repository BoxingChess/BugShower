name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - '.github/**'
      - 'AutoPR/**'
      - 'SETUP_GUIDE.md'

permissions:
  contents: write
  pull-requests: write

jobs:
  # ── Job 1: PR 설명 자동 생성 ─────────────────────────────────────────────
  ai-description:
    name: Generate PR Description
    runs-on: ubuntu-latest
    if: |
      github.event.action == 'opened' ||
      github.event.pull_request.body == '' ||
      github.event.pull_request.body == null
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install anthropic requests

      - name: Generate PR description
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          REPO: ${{ github.repository }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          BASE_BRANCH: ${{ github.event.pull_request.base.ref }}
        run: python AutoPR/ai_pr_description.py

  # ── Job 2: AI 코드 리뷰 ──────────────────────────────────────────────────
  ai-review:
    name: Claude AI Code Review
    runs-on: ubuntu-latest
    needs: [ai-description]
    if: always()
    outputs:
      bug_level: ${{ steps.review.outputs.bug_level }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install anthropic requests

      - name: Run AI Code Review
        id: review
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: python AutoPR/ai_review.py

  # ── Job 3: 담당자 부재 시 자동 승인 & Merge ──────────────────────────────
  # PR 담당자(Assignee)가 REVIEWER_ABSENT_LIST에 포함된 경우에만 실행
  ai-auto-approve:
    name: Auto Approve (Reviewer Absent)
    runs-on: ubuntu-latest
    needs: [ai-review]
    if: |
      always() &&
      (needs.ai-review.outputs.bug_level == 'none' ||
       needs.ai-review.outputs.bug_level == 'style')
    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Auto Approve & Merge (if reviewer absent)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_SHA: ${{ github.event.pull_request.head.sha }}
          REPO: ${{ github.repository }}
          BUG_LEVEL: ${{ needs.ai-review.outputs.bug_level }}
          # 현재 부재 중인 사람 목록 (Toggle My Absence 워크플로우로 자동 관리)
          REVIEWER_ABSENT_LIST: ${{ vars.REVIEWER_ABSENT_LIST }}
        run: python AutoPR/ai_auto_approve.py
